export default function Banner() {
      return(
            <div>
                  <header>Orbit Report</header>
                  <p>Click on the buttons to see the satellites by Orbit Type!</p>
            </div>
      );
}

